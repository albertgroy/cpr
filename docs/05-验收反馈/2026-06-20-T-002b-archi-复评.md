# T-002b 阻塞修复复评（archi）

- 日期：2026-06-20
- 评审者：archi
- CCB 任务：job_d19039809567（来自 ccb_self；上游 job_39ad928961 终评 → 阻塞修复 → 本次复评）
- 分支：`feat/T-002b-client`
- 复评 HEAD：`74661d7 fix(T-002b): wire main to renderer and danger config`
- 修复方：agent1（job_6f13cf908ae5）
- 上次报告：`docs/05-验收反馈/2026-06-20-T-002b-archi-终评.md`
- 复评范围（上次裁定，仅这 4 条）：B-1 / B-2 / B-4 / en-US locale 切换

---

## 1. 复评结论

**通过（修改后通过）。无阻塞条款。合并就绪。**

agent1 的修复 commit `74661d7` 闭合了上次评审标记的 3 条阻塞项（B-1 / B-2 / B-4），并把 en-US locale 切换打通。生产代码 `cpr/cli/*` 已与 `cpr/cli/render.py` 公开 API 对齐，与协议 v1 §6 `confirm_danger` 三档语义一致，与 §8 测试三件套布局一致。

---

## 2. 4 条复评项逐项核查

| 编号 | 项目 | 状态 | 关键证据 |
|---|---|---|---|
| B-1 | render.py 公开 API 接线，ad-hoc 渲染删除 | ✅ 通过 | `cpr/cli/main.py:19` 导入 `confirm_danger / render_error_payload / render_no_args / render_quota_status / render_response`；`main.py:39` 调 `render_no_args`；`main.py:63` 调 `render_response`；`main.py:109` 调 `confirm_danger`；`main.py:228-234` `_render_api_error` 包 `render_error_payload`；`main.py:247` 调 `render_quota_status`。本地 `_error_message` 已删；本地 `render_response` 已删；剩余 `_render_exec_command`（main.py:133）是 argv 占位符插值（不是本地化文案），保留合理。所有 `ApiError(...)` 抛出处（main.py:46/80/86/150/153/156/225）`message=""`，依赖 render 层按 code 取本地化 title。 |
| B-2 | `client.confirm_danger always\|once\|never` 真接线 | ✅ 通过 | `main.py:185` 默认 config 含 `"confirm_danger": "always"`；`main.py:214-216` `_confirm_mode` 读取 + clamp 到 `{always, once, never}`，越界回 `always`；`main.py:64` 把 `_confirm_mode(config)` 传入 `_maybe_execute`；`main.py:89-118` 三档语义：`never` → `skip_prompt=True`；`once` → 命中 `_CONFIRMED_DANGER:set[(tool, exec_template)]` 时 skip，确认后入集合（main.py:111-112）；`always` → 默认 y/N。`_CONFIRMED_DANGER` 是模块级 set（main.py:26），与协议 v1 §6"v1 仅进程内 set 不持久化"一致。 |
| B-4 | `_fixture_help` 移出生产路径 | ✅ 通过 | `grep -rn "_fixture_help\|fixture_help" cpr/` → 0 命中。函数迁至 `tests/cli/conftest.py` 作 pytest fixture（`@pytest.fixture def fixture_help`）。生产 `find_help`（main.py:73-86）只走真实 subprocess 调 `<tool> ... --help`。 |
| en-US locale 切换 | stderr / 渲染输出真英文 | ✅ 通过 | `CPR_LOCALE=en-US` 跑 `unknown-tool-xxx` → "Unknown command or tool not installed: unknown-tool-xxx" + 英文 hint，exit=2；`CPR_LOCALE=zh-CN` 跑同一命令 → 中文。`render_error_payload` 走 `I18n(locale).t("error.<CODE>.title"/"hint")`；`resolve_locale` 顺序 `CPR_LOCALE > config.client.locale (skip "auto") > LANG > en-US` 与 §6 一致。 |

---

## 3. 测试与覆盖率

agent1 自验：

- `pytest` → 99 passed（上次终评时 94 passed，新增 5 条覆盖 confirm_danger 三档 + EOF 兜底 + en-US 切换）
- `cpr.cli` 包整体覆盖率 95.01%
  - `main.py` 94%
  - `render.py` 96%
  - `redact.py` 100%
  - `cache.py` 100%
  - `api.py` 86%
  - `executor.py` 89%

复评抽查未发现回归。Fixture 三件套布局（4 scenarios + 11 errors）保持完整，11 错误码契约测试（`test_error_fixture_codes`）继续通过。

---

## 4. 残留非阻塞观察（不计入本次裁定）

`cpr/cli/api.py` 三处仍持有 zh-CN 文案作 `ApiError.message`：

- `api.py:58` `raise ApiError("LLM_TIMEOUT", "AI 解析超时") from exc`
- `api.py:62` `raise ApiError(code, "server 不可达，请检查 ~/.cpr/config") from exc`
- `api.py:69` `raise ApiError("NETWORK_UNREACHABLE", "server 不可达，请检查 ~/.cpr/config") from exc`

实际是否泄漏到用户输出？复核 `cpr/i18n/en-US.yaml`：

- `error.LLM_TIMEOUT.title = "AI parse timed out (>{timeout}s)"` — 不含 `{message}` 占位符
- `error.NETWORK_UNREACHABLE.title = "AI server is unreachable; check server.endpoint in ~/.cpr/config."` — 不含 `{message}` 占位符

`render_error_payload` 不会拼接这三条 `message` 到最终输出。**当前用户视图全英文/全中文一致**。但代码侧文案不该硬编码 zh-CN，建议清理。

- 严重度：P3（cosmetic / 风格）
- 影响：协议 v1 §0 行无；用户可见输出无；仅未来若 i18n 模板加 `{message}` 占位符或 `--diag` 输出 raw error 时才会被看见
- 处理建议：进 `docs/06-待优化/T-002a-后续打磨.md` 同章节作为 T-002b 收尾项；不在本次合并门槛内

不在本次裁定范围。不阻塞合并。

---

## 5. 合并就绪度

- 协议 v1 §0~§9 契约符合 ✅
- `docs/02-系统设计/2026-06-19-AI起手式-架构与协议.md` 与实现一致 ✅
- 错误码 11 条全覆盖（fixture + 渲染断言）✅
- exec_template / candidates 安全校验（INVALID_TEMPLATE 双侧自验）✅
- confirm_danger 三档 + `--yes` + EOF/无 stdin 默认 N 的 P2-T002b-1 裁定已落地 ✅
- P1-T002b-2 base URL endpoint 决策（`DEFAULT_ENDPOINT="http://127.0.0.1:18888"` + `/resolve` 后缀自动 strip + 首启 config 不写 endpoint）已落地 ✅
- P3-T002b-3 mock 双语 fixture 推迟到 T-003，T-002b 不动 mock —— 与上次终评一致

**合并就绪。建议 ccb_self 走"协调者按用户授权合并"流程。**

---

## 6. 后续 backlog（建议 ccb_self 转入 `docs/06-待优化/`）

- P3 · 清理 `cpr/cli/api.py:58/62/69` 三处 zh-CN ApiError.message → 改空串或英文常量，依赖 render 层走 i18n。

—— archi 落字，2026-06-20。
