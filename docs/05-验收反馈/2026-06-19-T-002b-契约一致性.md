# T-002b 契约一致性自检（agent4 / 2026-06-19）

## 1. fixture 清单

- `pytest tests/cli/test_fixtures.py -v`：2 passed。
- scenarios: 4 条
  - `sdk_root`
  - `sdk_install_java`
  - `git_status`
  - `kubectl_apply`
- errors: 协议 §8.1 约定的 5 条 mock 错误已覆盖；协议 §2.1 错误码表 11 条未全量覆盖。
  - 已覆盖：`QUOTA_EXCEEDED`、`LLM_TIMEOUT`、`SERVER_ERROR`、`INVALID_TEMPLATE`、`SCHEMA_MISMATCH`
  - 缺少 fixture 目录：`HELP_NOT_FOUND`、`HELP_TIMEOUT`、`BAD_REQUEST`、`INVALID_CLIENT`、`LLM_PARSE_FAILED`、`NETWORK_UNREACHABLE`

## 2. 语义子集核对（协议 §8.3）

| 字段 | 判定 | 说明 |
| --- | --- | --- |
| `schema_version` 全等 | ✅ | 所有 request/response fixture 均为 `"1"`，`tests/cli/test_fixtures.py::test_fixture_three_file_layout` 已断言 request 侧。 |
| `result.danger` 全等 | ✅ | 4 个 scenario 均含 `result.danger`；`sdk_install_java` / `kubectl_apply` 为 true，`sdk_root` / `git_status` 为 false。 |
| `result.exec_template` 全等（如 fixture 有） | ✅ | `sdk_install_java`、`kubectl_apply` 含 `exec_template`，可供 T-003 做全等校验。 |
| `result.exec_shell_mode` 全等（如 fixture 有） | ✅ | `sdk_install_java` 为 `bash-lc-source`，`kubectl_apply` 为 `direct`。 |
| `result.candidates[].token` 集合 ⊇ fixture | ✅ | 每个 scenario 至少包含候选 token，可作为 T-003 真服务语义子集下界。 |
| `summary` / `notes` 不校验 | ✅ | fixture 保留文本，但按协议不要求真服务文本全等。 |
| 错误场景 `error.code` 全等 | ⚠️ | 现有 5 个错误场景可校验；协议 §2.1 其余 6 个错误码没有 errors fixture。 |

## 3. 错误码表与实现核对（协议 §2.1）

| 错误码 | HTTP | 退出码 | fixture 有 | 实现路径 | 判定 |
| --- | --- | --- | --- | --- | --- |
| `HELP_NOT_FOUND` | - | 2 | ❌ | `cpr/cli/main.py:86` 抛出；`cpr/cli/api.py:11` 映射退出码 | 实现有，fixture 缺 |
| `HELP_TIMEOUT` | - | 2 | ❌ | `cpr/cli/main.py:79` 捕获 timeout；`cpr/cli/api.py:12` 映射退出码 | 实现有，fixture 缺 |
| `BAD_REQUEST` | 400 | 5 | ❌ | `cpr/cli/api.py:17` 映射退出码；HTTP 错误读取在 `cpr/cli/api.py:47` | 映射有，fixture 缺 |
| `INVALID_CLIENT` | 422 | 5 | ❌ | `cpr/cli/api.py:18` 映射退出码；HTTP 错误读取在 `cpr/cli/api.py:47` | 映射有，fixture 缺 |
| `INVALID_TEMPLATE` | 422 | 5 | ✅ | `cpr/cli/main.py:147` 自查；`cpr/cli/api.py:19` 映射退出码 | 通过 |
| `SCHEMA_MISMATCH` | 426 | 5 | ✅ | `cpr/cli/main.py:223` 自查；`cpr/cli/api.py:20` 映射退出码 | 通过 |
| `QUOTA_EXCEEDED` | 429 | 4 | ✅ | `cpr/cli/api.py:16` 映射退出码；文案在 `cpr/cli/main.py:228` | 通过 |
| `LLM_PARSE_FAILED` | 502 | 5 | ❌ | `cpr/cli/api.py:21` 映射退出码；HTTP 错误读取在 `cpr/cli/api.py:47` | 映射有，fixture 缺 |
| `LLM_TIMEOUT` | 504 | 3 | ✅ | `cpr/cli/api.py:14` 映射退出码；timeout/URLError 分支在 `cpr/cli/api.py:51` | 通过 |
| `SERVER_ERROR` | 500 | 3 | ✅ | `cpr/cli/api.py:15` 映射退出码；HTTP fallback 在 `cpr/cli/api.py:66` | 通过 |
| `NETWORK_UNREACHABLE` | - | 3 | ❌ | `cpr/cli/api.py:13` 映射退出码；URLError 分支在 `cpr/cli/api.py:53` | 实现有，fixture 缺 |

## 4. exec_template 安全（P0-2）

- 占位符正则强制位置：`cpr/cli/main.py:153` 对 `{...}` 内容执行 `^[a-z][a-z0-9_]*$` 校验；`cpr/cli/main.py:156` 还要求占位符必须存在于 `exec_template_args`。
- 字符黑名单强制位置：`cpr/cli/main.py:23` 定义占位符匹配，`cpr/cli/main.py:24` 定义 `[;&|<>$`\\\n\r]` 黑名单，`cpr/cli/main.py:151` 同时检查黑名单与可打印 ASCII。
- 测试覆盖：
  - `tests/cli/test_main_api.py::test_validate_template_blocks_shell_meta_and_bad_placeholder` 覆盖 `;`、非法占位符 `{Bad}`、缺失 `exec_template_args`。
  - `tests/cli/test_more_main.py::test_main_miss_invalid_template` 覆盖 server 2xx 返回恶意 template 后 client 拒绝并退出 5。
  - `tests/fixtures/server/errors/invalid_template/response.json` 提供 mock fixture。

## 5. 缺口与建议

- 必须补：若 archi 要求“协议 §2.1 11 条错误码均有 `errors/` 三件套”，需补 6 个目录：`help_not_found`、`help_timeout`、`bad_request`、`invalid_client`、`llm_parse_failed`、`network_unreachable`。
- 可选打磨（入 `docs/06-待优化/`）：
  - `tests/cli/test_fixtures.py` 目前只断言三件套存在与部分 error code，可增加自动遍历 response 的 §8.3 语义字段校验。
  - `cpr/cli/main.py` 中 `_fixture_help` 是为 mock/smoke 提供的本地 fallback，可在后续拆为测试模式或 mock adapter，避免生产路径语义混杂。

## 6. 总结

建议进入 archi 终评：否。原因是协议 §8.1 的 5 个 mock 错误已覆盖，但本次自检明确要求对照协议 §2.1 的 11 条错误码表，当前 `errors/` fixture 还缺 6 条；建议补齐后再进入终评。
