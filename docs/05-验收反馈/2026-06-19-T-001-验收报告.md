# T-001 SDKMAN MVP 验收报告（草稿，2026-06-19）

## 任务

- 任务 ID：T-001
- 标题：SDKMAN MVP 命令构造器
- 关联契约：
  - `docs/02-系统设计/2026-06-19-接口契约.md`
  - `docs/02-系统设计/2026-06-19-契约逻辑与验收标准.md`
  - `docs/02-系统设计/2026-06-19-契约评审-archi.md`
  - `docs/02-系统设计/2026-06-19-契约补丁-v1.md`（agent1 commit `c6775ca`）
- 分支：`feat/T-001-sdkman-mvp`，未 push，未合 main。

## 提交清单（按 archi 提交顺序）

| # | hash | 作者 | 说明 |
|---|---|---|---|
| 1 | `ba4311a` | agent1 | feat(core): add command node and session models |
| 2 | `43a3a18` | agent1 | feat(core): add static candidate provider |
| 3 | `71b2644` | agent1 | feat(core): add executor with bash-lc-source mode |
| 4a | `e4a1af4` | agent1 | feat(data): add sdkman command tree yaml loader |
| 4b | `2281f97` | agent2 | feat(data): add sdkman command tree yaml |
| 4c | `872e16c` | agent2 | test(data): add sdkman.yaml structure assertions |
| 5 | `542df8b` | agent2 | feat(tui): prompt workspace layout (input + content + candidates) |
| 6 | `4f32273` | agent1 | feat(core): add dynamic candidate provider for sdk list java identifiers |
| 7 | `6d9b693` | agent1 | feat(core): add slash command parser with back clear help |
| 8 | `3f53baf` | agent2 | feat(tui): wire enter/backspace/esc/tab/arrow keybindings |
| 9 | `c6775ca` | agent1 | chore(docs): contract patch v1 covering identified gaps |
| 10 | `5eaa18b` | agent1 | chore(scripts): manual sdk smoke script |
| README | `16524ec` | agent2 | docs(readme): add MVP usage and platform notes |
| integration | `660f46b` | ccb_self | feat(tui): integrate slash parser and align param candidate source |

## 自动化验证（macOS ARM64，本仓库内）

```bash
$ python -m pytest -q
..............................                                           [100%]
30 passed in 0.08s

$ python -m pytest --cov=cpr.core --cov-fail-under=85 -q
TOTAL coverage 88.04%   Required 85% reached.

$ python -m cpr --check-tree
log: ~/.cpr/logs/cpr.log
ok: 14 nodes

$ python -m cpr --slash-parse "/ai 帮我安装 Java 17 LTS"
name=ai args=帮我安装 Java 17 LTS
```

`data/sdkman.yaml`（14 nodes）通过 `cpr.core.model.load_command_tree_file` 加载链接成功；param 子节点候选 `source="dynamic"`（已纠正 `dynamic-pending` 占位）。

## 契约符合度（覆盖 archi 评审 1.x 各节）

| 项 | 状态 | 证据 |
|---|---|---|
| CommandNode `id` 正则 / 不变量 / parse-link 两阶段 | ✓ | `cpr/core/model.py` + `tests/core/test_model_session.py` |
| CommandSession 最长前缀 + `unparsedTail` + 环形缓冲 | ✓ | `cpr/core/session.py` + 单测 |
| Backspace 仅在输入区空时弹 token；Esc 非空清空 / 空弹 | ✓ | `cpr/tui/keys.py` `_backspace` / `_escape` |
| CandidateProvider async + ttl + 错误结构 | ✓ | `cpr/core/candidates.py`、`cpr/tui/candidates.py` |
| 修改环境的 Executor 调用强制失效缓存 | 待 T-003 集成验证 | DynamicCandidateProvider 已声明 invalidate；TUI 当前未触发 install/use/default 流 |
| Executor 超时 / 编码 / env 白名单 / SDKMAN_DIR / cancel | ✓ | `cpr/core/executor.py` + mock subprocess 单测 |
| SlashCommand 文法 / `/back` 共享 reducer / `/clear` 仅清显示 | ✓ | `cpr/core/slash.py` + `cpr/tui/app.py:run_app` |
| 加载失败 → 退出码 2 | ✓ | `cpr/__main__.py` |
| locale 解析 `CPR_LOCALE` > 配置 > `LANG` > `en-US` | ✓ | `cpr/i18n/loader.py` |
| 日志路径 `~/.cpr/logs/cpr.log` 启动打印 | ✓ | `cpr/__main__.py` + `cpr/tui/app.py:_print_log_path` |
| Windows 友好退出 | ✓ | `cpr/tui/app.py:_refuse_on_windows` |

## 开放问题（不阻塞合并）

| 项 | 状态 |
|---|---|
| agent2 反馈"slash dispatch 未接入 run_app" | 已在 integration commit `660f46b` 解决 |
| agent2 反馈"`dynamic-pending` 不在枚举内" | 已在 integration commit `660f46b` 改为 `dynamic` |
| agent2 反馈"`--check-tree` / `--slash-parse` 入口缺失" | 实际已在 `cpr/__main__.py` 提供，无需改动 |
| 动态候选 fixture 与真实 SDKMAN 输出漂移 | P2，已在变更与缺陷队列；不阻塞 T-001 |
| 英文文案为 placeholder + warn | T-002 范围 |

## 待补：手工 SDKMAN 全流程冒烟

按 archi 验证清单，T-001 终验前还需在真实 TTY 上执行：

- macOS ARM64：`bash scripts/manual_sdk_smoke.sh`
- Ubuntu ARM64：同上

CCB 当前会话不是真实 TTY（agent2 已说明 `Input is not a terminal`），需要由用户在终端执行。冒烟脚本本身已通过 agent1 commit `5eaa18b` 交付。

## 不通过条件检查

- Executor 静默吞错：未发现，分流保留 stderr。
- 网络不可达静默 `ok=true`：执行器 `ok` 由 returncode 决定，未发现。
- 主循环 `sdk list java` ≥1s 卡顿：动态候选走 `run_in_executor`，UI 不阻塞；真实卡顿验证留手工冒烟。
- i18n fallback 缺失：覆盖。
- 引入禁止依赖：`pyproject.toml` 仅含 `prompt_toolkit / pyyaml`，dev 仅 `pytest / pytest-asyncio / pytest-cov`，符合。

## 合并就绪度

- 自动化验收通过、契约补丁 v1 入库、integration 已闭环。
- **唯一待办**：用户在真实 TTY 上跑一次 `bash scripts/manual_sdk_smoke.sh`，并把输出贴到本报告"## 手工冒烟记录"段。
- 然后由 archi 终验签字。
