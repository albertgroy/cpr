# T-002b 修复复测（agent4 / 2026-06-19）

## 环境

- 分支 / commit：`feat/T-002b-client` / `94d4510`
- python 路径：`/opt/homebrew/opt/python@3.11/bin/python3.11`（测试运行显示 Python 3.11.15）
- mock port：`23666`
- `CPR_HOME`：临时目录，配置 `server.endpoint` 指向本地 mock。

## 修复确认（agent2 commit 94d4510）

- pytest 92 passed：✅
- coverage ≥85：✅，`pytest --cov=cpr.cli --cov-fail-under=85` 通过，实际 `TOTAL 93.91%`。
- `_maybe_execute` 实现检查：`cpr/cli/main.py:123` 在非交互 stdin 时直接写 stderr 并返回 0；`cpr/cli/main.py:128` 捕获 `EOFError` 后同样按默认 N 返回 0。

## 4 条命令逐条

### 1. `printf '' | cpr sdk install java`

- 期望：exit=0；stderr 含拒绝文案；无 traceback。
- 实际：exit=0。
- stdout：

```text
cpr sdk install java
Install Java with SDKMAN
usage: sdk install java <identifier>
candidates:
  17.0.10-tem	Temurin 17 LTS
note: Downloads and writes into ~/.sdkman.
danger: writes to SDKMAN directories
```

- stderr：

```text
Non-interactive context: refused by default (pass -y / --yes to execute)
```

- ✅：EOF / 空管道按默认 N 拒绝执行，无 traceback。

### 2. `cpr sdk install java < /dev/null`

- 期望：exit=0；stderr 含拒绝文案；无 traceback。
- 实际：exit=0。
- stdout：

```text
cpr sdk install java
Install Java with SDKMAN
usage: sdk install java <identifier>
candidates:
  17.0.10-tem	Temurin 17 LTS
note: Downloads and writes into ~/.sdkman.
danger: writes to SDKMAN directories
```

- stderr：

```text
Non-interactive context: refused by default (pass -y / --yes to execute)
```

- ✅：`/dev/null` 非交互 stdin 按默认 N 拒绝执行，无 traceback。

### 3. `cpr unknown-tool-xxx`

- 期望：exit=2（`HELP_NOT_FOUND` 回归）。
- 实际：exit=2。
- stdout：

```text

```

- stderr：

```text
未识别的命令 / 工具未安装：unknown-tool-xxx
```

- ✅：`HELP_NOT_FOUND` 退出码与文案未被修复影响。

### 4. `cpr __quota`

- 期望：exit=4（`QUOTA_EXCEEDED` 回归）。
- 实际：exit=4。
- stdout：

```text

```

- stderr：

```text
今日 AI 额度已用完（50/50），8 小时后重置；可配置 ~/.cpr/config 自部署
```

- ✅：`QUOTA_EXCEEDED` 退出码与文案未被修复影响。

## 结论

- 4/4 通过。
- EOF / 无 stdin 危险确认已符合 `docs/06-待优化/T-002a-后续打磨.md` 的实施裁定：默认 N、不执行、stderr 提示、exit=0。
- 建议进入 archi 终评：是。
