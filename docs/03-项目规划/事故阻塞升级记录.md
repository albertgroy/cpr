# 事故阻塞升级记录

| 日期 | 类型 | 现象 | 影响 | 已尝试 | 当前负责人 | 需要决策 | 处理状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-06-19 | 产品形态走错 | T-001 验收通过后用户首次实测 `python -m cpr`，发现实际形态（prompt_toolkit 全屏工作台 + 本地 yaml 命令树）与意图（`cpr <tool>` 前置式 CLI + AI 知识库）不符 | T-001 形态 A 不可继续；分支 `feat/T-001-sdkman-mvp` 不合并；产品方向改为 AI 起手式（client 调 server 端 LLM 解析 help_text + 缓存） | (1) 用户当面确认形态 B；(2) 11 条形态决策落字；(3) 用户进一步否决"本地 yaml 命令树"路径，确认 AI 必选；(4) 5 条架构决策落字 | ccb_self（协调） + archi（评审 T-002a） | T-002a 协议契约由 archi 评审；通过后派 T-002b client；T-003 server 后续启动 | 处理中（已写入需求 v2 / 协议文档；T-001 残值评估完成；待 archi 评 T-002a） |
| 2026-06-19 | provider 不可用 | 第二开发对中的 kiro-cli provider 启动失败 | 第二开发对仅剩 agent3（codex）单兵；T-003 server 任务由 agent3 + ccb_self 共同承担；并发开发能力下降 | （未深入诊断，用户告知）| ccb_self | 是否在 T-003 启动前修复 kiro-cli / 是否切换其他 provider 补位 | 待诊断（T-003 启动前必须给出方案） |

## 升级触发条件

- 重复（≥2 次）测试失败且原因不明。
- CCB 通信异常：ask 无响应 / 回调丢失 / 队列阻塞。
- Provider 故障：claude / opencode / codex / kiro-cli 中任一不可用。
- 发布失败或回滚。
- 需求与契约冲突无法协调。

## 处理路径

- 先记录现象与影响，避免直接重启或清空上下文。
- 诊断 CCB 状态（参考 `ccb-self-diagnose` 技能）。
- 决策选项：暂停 / 重试 / 重派 / 恢复 CCB 上下文 / 咨询用户。
- 不要随手使用清空记忆的重置路径（如 `ccb -n`），除非用户明确选择。
