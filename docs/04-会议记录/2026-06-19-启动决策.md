# 启动决策（2026-06-19）

## 与会

- 用户（产品负责人 / 决策）
- ccb_self（协调者，记录）

## 议题与结论

### 1. 实现语言与 TUI 技术栈

- 决策：**Python + prompt_toolkit**。
- 理由：贴合“输入区 + 候选 + 多行说明”的命令构造器交互；上手最快；与原型阶段循环验证节奏匹配。
- 影响范围：`docs/03-项目规划/任务规划.md` 阶段 2、`docs/02-系统设计/2026-06-19-接口契约.md`、T-001 任务卡片。
- 后续：T-001 工程骨架以 Python 包结构组织；依赖加入 `prompt_toolkit`，按依赖变更规约提供 lockfile（`uv.lock` 或 `requirements.txt` 二选一，由实现对在派发回执中确认）。

### 2. 仓库初始化

- 决策：**现在 git init**，提交首个 commit（仅含已有规划与文档骨架），为 T-001 任务分支 `feat/T-001-sdkman-mvp` 做准备。
- 配套：`.gitignore` 屏蔽 `.ccb/.claude-*-session`、`.ccb/runtime/`、`.ccb/ccbd/`、`.ccb/shared-cache/`、`.DS_Store`；保留 `.ccb/ccb.config` 与 `.ccb/ccb_memory.md` 以保证 CCB 拓扑入仓。
- 版本号规约：见 `docs/03-项目规划/分支与发布规约.md`。

### 3. T-001 派发顺序

- 决策：**先由 archi 评审接口契约与验收标准，再派 T-001 给 agent1+agent2**。
- 理由：避免实现走偏后回滚；契约与验收口径需在派发前对齐。
- 评审范围：
  - `docs/02-系统设计/2026-06-19-接口契约.md`
  - `docs/02-系统设计/2026-06-19-契约逻辑与验收标准.md`
- archi 评审需输出：契约缺口清单、验收口径修订建议、是否阻塞 T-001 派发的判定。

### 4. 项目正式名

- 决策：**暂用代号 `cpr`，正式名延后**。
- 理由：体感验证未完成前敲定正式名风险较大；命名讨论作为平行议题保留。
- 后续：候选 `cmdpilot` / `cmdguide` / `cmdpath` 等留待 SDKMAN MVP 体感反馈后再议。

## 对滚动执行列表的影响

- T-001 状态保持“待分派”，新增前置：archi 契约评审。
- 新增 T-000（一次性启动项，不入滚动列表）：git init + 启动决策记录 + 委派契约评审。

## 风险与缓解

- 技术栈风险：prompt_toolkit 在 Windows 终端下行为差异 → MVP 主验证 macOS / Linux，Windows 留作后续兼容议题。
- 命名延后风险：CLI 二进制名暂未定 → MVP 阶段以 `python -m cpr` 启动，避免过早绑定二进制名。

## 后续动作

1. 写入 `.gitignore` 并完成首个 commit。
2. 通过 CCB ask 把契约评审任务派给 archi，附评审范围与判定要求。
3. 评审通过后，用 archi 给的修订更新契约文档与滚动列表，再派 T-001。
